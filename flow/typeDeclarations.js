// @flow
/* eslint-disable */
import type { Store } from 'redux';

declare type D2 = {
    models: Object,
    system: {
        settings: {
            all: () => Object,
        }
    }
};

declare type State = Object;